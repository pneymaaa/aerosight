import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#388e3c", // Hijau untuk lingkungan
    },
    secondary: {
      main: "#f57c00",
    },
  },
});

export default theme;
